<Window x:Class="TP2_GLII.Views.FenetreCatalogue"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TP2_GLII.Views"
        mc:Ignorable="d"
        Title="FenetreCatalogue" Height="600" Width="1000" Background="Black" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>

        <DockPanel Margin="10">

            <!-- Bande du haut (connexion / compte / etc.) -->
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="txtGreeting" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Button x:Name="BtnConnexion" Content="Se connecter" Margin="5" Padding="10,4"/>
                <Button x:Name="BtnInscription" Content="S'inscrire" Margin="5" Padding="10,4"/>
                <Button Content="Mon compte"
                    Width="130"
                    Height="36"
                    Click="BtnCompte_Click"/>
            </StackPanel>


            <!-- Zone principale -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">

                    <TextBox x:Name="txtRecherche" Width="350" Height="30" Margin="0,0,0,10"/>
                    <Button x:Name="BtnRechercher" Content="Rechercher" Width="120" Height="30" Margin="0,0,0,10"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="lvFilms">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" Margin="10"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#1A1A1A" CornerRadius="8" Margin="10" Width="180" Cursor="Hand">
                                        <StackPanel>
                                            <Image Source="{Binding Affiche}" Height="250" Stretch="Fill" />
                                            <TextBlock Text="{Binding Titre}" FontWeight="Bold" 
                                   Foreground="White" Margin="6,8,6,0" TextAlignment="Center"/>

                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,10">
                                                <Button Content="Détails" Width="70" Margin="4" Click="BtnDetails_Click"/>
                                                <Button Content="▶" Width="40" Click="BtnVisionner_Click"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect Color="Black" BlurRadius="5"/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="White" BlurRadius="15"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="RenderTransform">
                                                            <Setter.Value>
                                                                <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                </StackPanel>
            </ScrollViewer>

        </DockPanel>

    </Grid>




</Window>
