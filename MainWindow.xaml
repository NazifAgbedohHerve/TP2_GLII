<Window x:Class="TP2_GLII.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TP2_GLII"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="Black" Offset="0"/>
                <GradientStop Color="Gray" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="✨ BIENVENUE ✨" 
                      FontSize="32" 
                      FontWeight="Bold" 
                      Foreground="White"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,20"/>

            <TextBlock Text="Nous sommes ravis de vous accueillir" 
                      FontSize="18" 
                      Foreground="White"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,40"/>

            <Button x:Name="BtnEntrer" 
                    Content="Entrer dans l'application" 
                    Background="White" 
                    Foreground="#667eea"
                    FontSize="16" 
                    FontWeight="Bold"
                    Padding="30,15" 
                    Click="BtnEntrer_Click"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
